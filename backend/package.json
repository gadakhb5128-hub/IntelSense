{
  "name": "intelsense-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "mvn spring-boot:run",
    "start": "mvn spring-boot:run",
    "build": "mvn clean package -DskipTests"
  }
}
