server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/intelsense_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: tanish
    password: Tanish@2009
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: intelsense-secret-key-change-me
  expiration-ms: 86400000
